package oop.abstraction.abstraction2;

public interface CanSwim {

    //ALL VARIABLES WILL BE BY DEFAULT ABSTRACT, PUBLIC, FINAL

    boolean FIN = true;

    //methods
    //as a RETURN TYPE CAN BE ANYTHING, OBJECT OR ANYTHING ELSE

    Integer swim(int deep);
    //its abstract by default so we leave it unimplemented
}
